# M004 S01 Transition Contract Audit

> Generated from `tests/FrameFlow.Playback.Tests/Contracts/PlaybackContractInventory.cs`. Do not edit by hand; update the inventory and re-render this artifact instead.

## Status legend

- `supported` — current controller behavior is treated as owned contract and should be executable in tests.
- `documented-mismatch` — the current runtime or dispatch behavior diverges from configured or ideal semantics; keep the drift explicit for S02/S03.
- `deferred` — the command is intentionally queued until another in-flight operation drains; the row documents the required sequencing.

## Inventory summary

- Total rows: 33
- Supported rows: 31
- Documented mismatch rows: 0
- Deferred rows: 2

## Playback

| ID | Trigger kind | Source / context | Trigger | Destination / result | Guard / context | Status | Required side effects | Forbidden side effects | Signals | PlaybackController logs | Owner notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| PB001 | public-command | Idle | LoadAsync(source) | Paused via Loading → Loading → Loading | Valid only when the playback region can fire Load (Idle or Stopped). | supported | Create a session via IPlaybackSessionFactory, supplying a SessionCallbacks record at construction (ADR-0028 §4).<br/>Emit PlaybackStateChanged for Idle → Loading → Paused (public surface; internal substates Initializing/Preparing/InitialBuffering collapse to Loading per ADR-0027). | Do not emit ErrorOccurred on the successful load path.<br/>Do not start forward playback before an explicit PlayAsync command. | PlaybackStateChanged | Session created via factory<br/>Entering Initializing — loading source '{SourceName}'<br/>Entering Preparing — metadata already parsed during initialization<br/>Entering InitialBuffering — auto-chaining to BufferReady<br/>Entering Paused | — |
| PB002 | public-command | Paused | PlayAsync() | Playing | Requires the playback region to accept Play; the controller owns per-session activation truth and explicitly chooses first-play renderer startup versus later resume. | supported | Consult controller-owned per-session activation truth and call session.StartRenderersAsync() before the session has ever played, otherwise call session.ResumeAsync().<br/>Start the position ticker on entry to Playing.<br/>Emit PlaybackStateChanged for Paused → Playing. | Do not emit ErrorOccurred on the happy path.<br/>Do not route through Rebuffering on direct pause-to-play resumption. | PlaybackStateChanged | Playback state transition: {Source} → {Destination} via {Trigger}<br/>Entering Playing | — |
| PB003 | public-command | Playing | PauseAsync() | Paused | Immediate pause is valid unless a seek-drain row currently owns the command sequencing. | supported | Call session.PauseAsync().<br/>Stop the position ticker on exit from Playing.<br/>Emit PlaybackStateChanged for Playing → Paused. | Do not leave the controller in Rebuffering.<br/>Do not emit ErrorOccurred on the cooperative pause path. | PlaybackStateChanged | Playback state transition: {Source} → {Destination} via {Trigger}<br/>Entering Paused | — |
| PB004 | internal-trigger | Playing | InternalTriggerCommand(BufferUnderrun) | Rebuffering | BufferUnderrun is an internal pipeline callback routed through the controller command channel. | supported | Emit PlaybackStateChanged for Playing → Rebuffering.<br/>Leave the session running without injecting an explicit pause call. | Do not call session.PauseAsync() just because natural rebuffering started.<br/>Do not emit ErrorOccurred for a normal stall/recovery cycle. | PlaybackStateChanged | Playback state transition: {Source} → {Destination} via {Trigger}<br/>Entering Rebuffering — natural stall, waiting for refill | — |
| PB005 | internal-trigger | Rebuffering | InternalTriggerCommand(BufferReady) | Playing | BufferReady returns the controller to Playing without any controller-owned or runtime-owned playback reactivation path. | supported | Emit PlaybackStateChanged for Rebuffering → Playing.<br/>Restart the position ticker on entry to Playing. | Do not call session.StartRenderersAsync() or session.ResumeAsync() on this recovery path.<br/>Do not emit RepeatModeChanged or ErrorOccurred. | PlaybackStateChanged | Playback state transition: {Source} → {Destination} via {Trigger}<br/>Entering Playing | — |
| PB006 | public-command | Playing / Paused / Rebuffering | UnloadAsync() | Unloaded | Unload is valid from the active playback states; when seek drain is active, the deferred-command rows own the ordering details. | supported | Emit PlaybackStateChanged into Stopped.<br/>Dispose the active session on entry to Stopped.<br/>Leave any caller-owned sinks quiescent without disposing the sink objects.<br/>Leave the controller with no active session reference. | Do not keep the old session alive after Stop completes.<br/>Do not dispose caller-owned sink objects as part of terminal teardown.<br/>Do not emit ErrorOccurred on the normal stop path. | PlaybackStateChanged<br/>caller-owned audio sink deactivate counter | Playback state transition: {Source} → {Destination} via {Trigger}<br/>Entering Unloaded — disposing session<br/>Disposing session | — |
| PB007 | internal-trigger | Loading / Loading / Loading / Paused / Playing / Rebuffering / Ended | InternalTriggerCommand(FatalError) | Error | FatalError is parameterized through PlaybackError and is only meaningful while the playback region can still fire the error trigger. | supported | Dispose the active session on entry to Error.<br/>Leave any caller-owned sinks quiescent without disposing the sink objects.<br/>Emit ErrorOccurred with the forwarded PlaybackError.<br/>Emit PlaybackStateChanged for the state that faulted into Error.<br/>Leave the controller with no active session reference. | Do not silently swallow the fatal playback error.<br/>Do not dispose caller-owned sink objects as part of terminal teardown.<br/>Do not leave the controller in a non-error playback state after the fatal trigger is accepted. | PlaybackStateChanged<br/>ErrorOccurred<br/>caller-owned audio sink deactivate counter | Playback state transition: {Source} → {Destination} via {Trigger}<br/>Entering Error — category: {Category}, message: {ErrorMessage}<br/>Disposing session | — |
| PB008 | playback-trigger | Error / Unloaded | Reset | Idle | Reset is a state-machine trigger used to return from Error or Stopped to Idle. | supported | Emit PlaybackStateChanged for Error → Idle or Stopped → Idle.<br/>Leave the controller with no active session reference. | Do not recreate a session during Reset.<br/>Do not emit ErrorOccurred while successfully resetting. | PlaybackStateChanged | Playback state transition: {Source} → {Destination} via {Trigger} | — |

## Seek

| ID | Trigger kind | Source / context | Trigger | Destination / result | Guard / context | Status | Required side effects | Forbidden side effects | Signals | PlaybackController logs | Owner notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| SK001 | public-command | Paused / Playing with active session | SeekAsync(position) | Result.Ok with SeekPending → SeekInProgress | Seek acceptance currently depends only on an active session; the public command does not consult playback CanFire(Seek). | supported | Emit SeekStateChanged for NotSeeking → SeekPending → SeekInProgress.<br/>Start a background seek runner and pass a cancellable token to session.SeekAsync(position).<br/>Complete the public SeekAsync task as accepted before the background seek necessarily finishes. | Do not emit ErrorOccurred on acceptance alone.<br/>Do not require playback-state motion just to accept the seek command. | SeekStateChanged | Seek state transition: {Source} → {Destination} via {Trigger}<br/>Seek accepted — operation: {OperationId}, position: {PositionSeconds}s | — |
| SK002 | seek-outcome | SeekInProgress | SeekOutcomeCommand(success) | NotSeeking | The outcome must match the active seek operation id before it is allowed to complete the seek region. | supported | Emit SeekStateChanged for SeekInProgress → NotSeeking.<br/>Log seek completion for the active operation id.<br/>Release any queued deferred Pause/Stop commands after the seek drain finishes. | Do not emit ErrorOccurred on a successful seek outcome.<br/>Do not keep the active seek cancellation source installed after completion. | SeekStateChanged | Seek state transition: {Source} → {Destination} via {Trigger}<br/>Seek completed — operation: {OperationId} | — |
| SK003 | seek-outcome | Active seek replaced by a newer seek or canceled for seek drain | SeekOutcomeCommand(cancelled) | NotSeeking | Cancellation is cooperative and non-fatal when the outcome belongs to the active operation id. | supported | Emit SeekStateChanged back to NotSeeking for the active operation.<br/>Log cooperative cancellation instead of surfacing ErrorOccurred.<br/>Allow queued deferred commands to continue after the cancellation-driven seek drain completes. | Do not transition playback into Error for an expected cancellation.<br/>Do not fail queued Pause/Stop commands just because the seek was canceled cooperatively. | SeekStateChanged | Seek cancelled — operation: {OperationId}<br/>Playback trigger {Trigger} proceeding after seek drain — operation: {OperationId} | — |
| SK004 | seek-outcome | SeekInProgress | SeekOutcomeCommand(error) | NotSeeking and, when permitted, Error | A faulted seek first drains the seek region, then fires FatalError only if the playback region can still accept it. | supported | Emit SeekStateChanged for SeekInProgress → NotSeeking.<br/>Log the seek fault with the active operation id.<br/>Emit ErrorOccurred and transition playback to Error when FatalError can fire.<br/>Fail any queued deferred commands with a system error result. | Do not treat a real seek fault as a stale or malformed outcome.<br/>Do not leave queued deferred commands hanging after the fault path completes. | SeekStateChanged<br/>ErrorOccurred<br/>PlaybackStateChanged | Seek faulted — operation: {OperationId}, message: {ErrorMessage}<br/>Entering Error — category: {Category}, message: {ErrorMessage} | — |
| SK005 | seek-outcome | Seek outcome whose operation id is not the active seek operation id | SeekOutcomeCommand(stale) | Ignored with no state change | Stale outcomes are a normal consequence of replacing in-flight seeks. | supported | Ignore the stale outcome without changing playback or seek state.<br/>Log the stale operation id and the active operation id for diagnosis. | Do not emit ErrorOccurred for a stale completion or stale cancellation.<br/>Do not complete the active seek region from an old operation id. | None. | Ignoring stale seek outcome — operation: {OperationId}, active: {ActiveOperationId} | — |
| SK006 | seek-outcome | Seek outcome with operationId <= 0 or both cancelled and errored | SeekOutcomeCommand(malformed) | Ignored with no state change | Malformed outcomes are treated as contract-definition defects rather than repaired at runtime. | supported | Ignore the malformed outcome without changing playback or seek state.<br/>Log the malformed outcome fields so the producer defect is observable. | Do not synthesize a replacement result for malformed outcome payloads.<br/>Do not move the seek region just because a malformed outcome arrived. | None. | Ignoring malformed seek outcome — operation: {OperationId}, cancelled: {WasCanceled}, hasError: {HasError} | — |

## Repeat

| ID | Trigger kind | Source / context | Trigger | Destination / result | Guard / context | Status | Required side effects | Forbidden side effects | Signals | PlaybackController logs | Owner notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| RP001 | public-command | Off | SetRepeatModeAsync(RepeatMode.One) | One | Repeat mode changes are handled by the orthogonal repeat region and do not require playback-state motion. | supported | Emit RepeatModeChanged for Off → One.<br/>Return Result.Ok once the repeat region transition completes. | Do not emit PlaybackStateChanged just because repeat mode changed.<br/>Do not emit ErrorOccurred on a normal repeat toggle. | RepeatModeChanged | Repeat mode transition: {Source} → {Destination} via {Trigger} | — |
| RP004 | public-command | Repeat mode already equals requested mode | SetRepeatModeAsync(currentMode) | Result.Ok with no repeat-region transition | The dispatch loop treats same-mode selections as success rather than invalid operations. | supported | Return Result.Ok without firing the repeat region. | Do not emit RepeatModeChanged when the requested mode already matches the current mode.<br/>Do not emit PlaybackStateChanged or ErrorOccurred. | None. | None. | — |

## Cross-region

| ID | Trigger kind | Source / context | Trigger | Destination / result | Guard / context | Status | Required side effects | Forbidden side effects | Signals | PlaybackController logs | Owner notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| XR001 | internal-trigger | Playing with Off | InternalTriggerCommand(LastFrameRendered) | Ended | LastFrameRendered reaches Ended only when repeat mode is Off. | supported | Emit PlaybackStateChanged for Playing → Ended.<br/>Leave loop count unchanged and do not seek to zero. | Do not emit LoopRestarted when repeat mode is Off.<br/>Do not leave playback in Playing after the end-of-stream trigger is accepted. | PlaybackStateChanged | Playback state transition: {Source} → {Destination} via {Trigger}<br/>Entering Ended | — |
| XR002 | internal-trigger | Playing with One | InternalTriggerCommand(LastFrameRendered) | Internal transition that stays in Playing | Repeat One converts end-of-stream into a loop restart instead of a playback-state transition. | supported | Increment the loop counter.<br/>Emit LoopRestarted with the new loop count.<br/>Call session.SeekAsync(TimeSpan.Zero). | Do not emit PlaybackStateChanged to Ended on the repeat-one loop path.<br/>Do not emit ErrorOccurred on a normal loop restart. | LoopRestarted | Loop restarted — count: {LoopCount}, mode: {Mode} | — |
| XR004 | internal-trigger | Session callback or internal pipeline event routed through InternalTriggerCommand | InternalTriggerCommand(BufferReady \| BufferUnderrun \| LastFrameRendered \| FatalError) | Non-error internal triggers are guarded by CanFire and silently dropped when stale; FatalError always fires | DispatchLoopAsync guards non-error internal triggers with CanFire so that stale triggers (e.g. LastFrameRendered arriving after a pause+seek stop) are dropped instead of throwing. FatalError bypasses the guard because error context must always reach the Error state. | supported | Guard non-error internal triggers with CanFire before firing against the playback state machine.<br/>Translate FatalError plus Exception into a PlaybackError before firing the parameterized error trigger.<br/>Log dropped stale triggers at Debug level. | Do not throw when an internal trigger is not permitted from the current state. | PlaybackStateChanged<br/>ErrorOccurred<br/>LoopRestarted | Internal trigger posted to command channel: {Trigger}<br/>Internal trigger dropped — channel full or closed: {Trigger}<br/>Dropping stale internal trigger {Trigger} — not permitted from {CurrentState} | Resolved: internal triggers are now guarded by CanFire to support pause+seek stop without crashing on stale EOS or BufferUnderrun callbacks. |
| XR005 | public-command | Idle / Unloaded | LoadAsync(source) when session.InitializeAsync throws | Result.Fail(System) with playback transitioning to Error before LoadAsync completes | LoadCommand observes controller-owned initialization failure after the load trigger completes so the public result agrees with the emitted Error transition. | supported | Log session initialization failure.<br/>Dispose the partially created session.<br/>Clear the controller's active session reference during failure cleanup.<br/>Emit ErrorOccurred and PlaybackStateChanged into Error before completing LoadAsync.<br/>Return Result.Fail(System) aligned with the resulting playback state. | Do not return Result.Ok after initialization has already faulted.<br/>Do not leave the partially created session undisposed after initialization fails.<br/>Do not leave the controller holding a stale session reference after failure cleanup. | PlaybackStateChanged<br/>ErrorOccurred | Session initialization failed: {ErrorMessage}<br/>Entering Error — category: {Category}, message: {ErrorMessage} | — |
| XR006 | public-command | Ended | SeekAsync(position) | Paused via Loading with SeekPending → SeekInProgress → NotSeeking | Public Ended-state seek first fires the playback-region Seek trigger so the controller re-enters the configured buffering path before the session seek runner executes. | supported | Fire the playback-region Seek transition from Ended into Loading.<br/>Auto-chain Loading → Paused through BufferReady before the seek runner completes.<br/>Emit SeekStateChanged for the public seek cycle and call session.SeekAsync(position). | Do not leave playback in Ended after a successful public seek acceptance.<br/>Do not bypass the configured playback transition path when Ended is the source state. | PlaybackStateChanged<br/>SeekStateChanged | Playback state transition: {Source} → {Destination} via {Trigger}<br/>Entering InitialBuffering — auto-chaining to BufferReady<br/>Entering Paused<br/>Seek accepted — operation: {OperationId}, position: {PositionSeconds}s | — |
| XR007 | internal-trigger | Playing with runtime demux or decode worker reporting a non-EOF failure | InternalTriggerCommand(FatalError) posted from OnWorkerFaulted | Error | Runtime producers must keep natural EOF on LastFrameRendered and post FatalError only for real worker faults; a non-EOF demux read failure therefore must remain distinct from normal completion. | supported | Route the worker failure through FatalError and emit PlaybackStateChanged for Playing → Error.<br/>Emit ErrorOccurred with the forwarded worker exception.<br/>Keep the failure observable as WorkerFaulted / FatalError instead of normal EOF handling. | Do not post LastFrameRendered for a non-EOF demux read failure.<br/>Do not transition playback to Ended when the worker fault path is taken. | PlaybackStateChanged<br/>ErrorOccurred | Internal trigger posted to command channel: {Trigger}<br/>Entering Error — category: {Category}, message: {ErrorMessage} | — |
| XR008 | public-command | Playing / Paused / Rebuffering with active session | DisposeAsync() | Controller teardown with no active session and no post-dispose callback ownership | DisposeAsync is a controller-lifetime operation rather than a playback trigger, so it must tear down runtime ownership without routing through Stopped or Error. | supported | Unload: stop the position ticker and dispose the active session.<br/>Clear the controller's active session reference. | Do not emit fresh PlaybackStateChanged or ErrorOccurred events just because disposal ran.<br/>Do not accept post-dispose callback invocations as live state-machine triggers — the command channel is closed and late callbacks must be no-ops (ADR-0028 §4). | PositionTick | PlaybackController disposed | — |
| XR009 | public-command | SeekInProgress with an active controller-owned seek runner | DisposeAsync() during in-flight seek | NotSeeking with late seek outcomes rendered unobservable | Disposal wins the race by canceling the active seek, draining any deferred seek-owned commands, and suppressing late seek outcomes for the disposed controller lifetime. | supported | Cancel the active seek and log the cancellation request with requestedBy = DisposeAsync.<br/>Normalize the seek region back to NotSeeking before disposal completes.<br/>Fail any queued deferred Pause/Stop commands instead of leaving them unresolved.<br/>Ignore late seek completion, cancellation, or fault outcomes after the controller has been disposed. | Do not emit ErrorOccurred when a late seek outcome arrives after disposal has already won the race.<br/>Do not leave the disposed controller stuck in SeekInProgress. | SeekStateChanged | Seek cancel requested — operation: {OperationId}, requested by: {RequestedBy}<br/>Ignoring seek outcome after controller disposal — operation: {OperationId}, cancelled: {WasCanceled}, hasError: {HasError} | — |
| XR010 | public-command | Ended | PlayAsync() | Playing via Unloaded → Loading → Loading → Loading → Paused | Replay is a controller-owned recovery path: the ended runtime is torn down and the last successfully loaded source is loaded again before playback is re-primed from the beginning. | supported | Route replay through Ended → Unloaded before rebuilding playback.<br/>Dispose the ended session and create a fresh session for the same loaded source.<br/>Emit PlaybackStateChanged for Ended → Unloaded → Loading → Loading → Loading → Paused → Playing.<br/>Call session.StartRenderersAsync() on the fresh session rather than session.ResumeAsync(). | Do not call session.ResumeAsync() on the ended session.<br/>Do not keep the ended session alive after replay recovery completes.<br/>Do not depend on stale completed-runtime ownership surviving Ended just to replay the item. | PlaybackStateChanged | Replaying ended item through recovery path — source: {SourceName}<br/>Entering Unloaded — disposing session<br/>Session created via factory<br/>Renderers started (first play) | — |

## Illegal commands

| ID | Trigger kind | Source / context | Trigger | Destination / result | Guard / context | Status | Required side effects | Forbidden side effects | Signals | PlaybackController logs | Owner notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| IL001 | public-command | Idle | PlayAsync() | Result.Fail(ErrorCategory.InvalidOperation) | Play is illegal from Idle because no media is loaded. | supported | Return Result.Fail with category InvalidOperation.<br/>Log the invalid operation with the current playback state. | Do not emit PlaybackStateChanged for a rejected PlayAsync from Idle.<br/>Do not emit ErrorOccurred for an expected invalid command. | None. | Invalid operation: cannot fire {Trigger} from state {CurrentState} | — |
| IL002 | public-command | Idle | PauseAsync() | Result.Fail(ErrorCategory.InvalidOperation) | Pause is illegal from Idle because there is no active playback session to suspend. | supported | Return Result.Fail with category InvalidOperation.<br/>Log the invalid operation with the current playback state. | Do not emit PlaybackStateChanged for a rejected PauseAsync from Idle.<br/>Do not emit ErrorOccurred for an expected invalid command. | None. | Invalid operation: cannot fire {Trigger} from state {CurrentState} | — |
| IL003 | public-command | Idle | UnloadAsync() | Result.Fail(ErrorCategory.InvalidOperation) | Unload is illegal from Idle because no session exists to unload. | supported | Return Result.Fail with category InvalidOperation.<br/>Log the invalid operation with the current playback state. | Do not emit PlaybackStateChanged for a rejected UnloadAsync from Idle.<br/>Do not emit ErrorOccurred for an expected invalid command. | None. | Invalid operation: cannot fire {Trigger} from state {CurrentState} | — |
| IL004 | public-command | No active session | SeekAsync(position) | Result.Fail(ErrorCategory.InvalidOperation) | SeekCommand is rejected when _session is null, regardless of playback-state configuration. | supported | Return Result.Fail with category InvalidOperation.<br/>Log the invalid operation against the current playback state. | Do not emit SeekStateChanged when the controller has no active session.<br/>Do not emit ErrorOccurred for the missing-session rejection. | None. | Invalid operation: cannot fire {Trigger} from state {CurrentState} | — |
| IL005 | public-command | Playing | LoadAsync(source) | Result.Fail(ErrorCategory.InvalidOperation) | Load is rejected when the playback region cannot fire Load from the current state. | supported | Return Result.Fail with category InvalidOperation.<br/>Log the invalid operation against the current playback state. | Do not create a second session while still playing.<br/>Do not emit PlaybackStateChanged for the rejected load command. | None. | Invalid operation: cannot fire {Trigger} from state {CurrentState} | — |
| IL006 | public-command | Unloaded | PlayAsync() | Result.Fail(ErrorCategory.InvalidOperation) | Stopped requires a new Load or Reset path before Play can become legal again. | supported | Return Result.Fail with category InvalidOperation.<br/>Log the invalid operation against the current playback state. | Do not emit PlaybackStateChanged for a rejected PlayAsync from Stopped.<br/>Do not resurrect the old session after Stop has already disposed it. | None. | Invalid operation: cannot fire {Trigger} from state {CurrentState} | — |

## Deferred commands

| ID | Trigger kind | Source / context | Trigger | Destination / result | Guard / context | Status | Required side effects | Forbidden side effects | Signals | PlaybackController logs | Owner notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| DF001 | public-command | Playing with SeekInProgress | PauseAsync() during in-flight seek | Deferred until seek drain completes, then Paused | RequiresSeekDrain() currently applies only to Pause and Stop while the seek region is in SeekInProgress. | deferred | Queue the Pause command instead of firing it immediately.<br/>Cancel the active seek and log the cancellation request.<br/>Emit SeekStateChanged back to NotSeeking before PlaybackStateChanged to Paused.<br/>Log that Pause proceeded after seek drain. | Do not call session.PauseAsync() before the seek drain completes.<br/>Do not emit ErrorOccurred for a cooperative seek cancellation that exists only to satisfy the deferred pause. | SeekStateChanged<br/>PlaybackStateChanged | Seek cancel requested — operation: {OperationId}, requested by: {RequestedBy}<br/>Seek cancelled — operation: {OperationId}<br/>Playback trigger {Trigger} proceeding after seek drain — operation: {OperationId} | S02/S03 owner note: preserve the explicit seek-drain-before-pause ordering if controller or pipeline authority moves; downstream tests should continue to assert NotSeeking before Paused. |
| DF002 | public-command | Playing with SeekInProgress | UnloadAsync() during in-flight seek | Deferred until seek drain completes, then Unloaded | RequiresSeekDrain() currently applies only to Pause and Stop while the seek region is in SeekInProgress. | deferred | Queue the Stop command instead of firing it immediately.<br/>Cancel the active seek and log the cancellation request.<br/>Emit SeekStateChanged back to NotSeeking before PlaybackStateChanged to Stopped.<br/>Dispose the session only after the deferred Stop fires.<br/>Log that Stop proceeded after seek drain. | Do not dispose the session before the seek drain completes.<br/>Do not emit ErrorOccurred for a cooperative seek cancellation that exists only to satisfy the deferred stop. | SeekStateChanged<br/>PlaybackStateChanged | Seek cancel requested — operation: {OperationId}, requested by: {RequestedBy}<br/>Seek cancelled — operation: {OperationId}<br/>Playback trigger {Trigger} proceeding after seek drain — operation: {OperationId}<br/>Entering Unloaded — disposing session | S02/S03 owner note: preserve the explicit seek-drain-before-stop ordering if controller or pipeline authority moves; downstream tests should continue to assert NotSeeking before Stopped and session disposal. |
